# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
out/
apps/*/dist/
apps/*/build/
apps/*/.next/
apps/*/out/

# Turborepo / pnpm cache
.turbo/
.pnpm-store/

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Environment / secrets — never committed, regardless of which app or
# how deep in the workspace. `.env.example` files (checked into every
# app on purpose, with placeholder values only) are explicitly kept.
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test
apps/*/.env
apps/*/.env.local
apps/*/.env.*.local
!.env.example
!apps/*/.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test / coverage output
coverage/
*.lcov

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Temporary files
*.tmp
*.temp
tmp/
temp/

# Local Mongo data volume (docker-compose mounts this on disk)
mnu_mongo_data/

# Uploaded menu item images (this task) — runtime data written by the
# API to local disk, not source. See apps/api/src/menu/menu.service.ts.
apps/api/uploads/
